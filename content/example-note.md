# Getting Started

1. Add your Obsidian notes to the `content/` directory
2. Edit `site.yaml` to configure your site
3. Push to GitHub — the workflow will build and deploy automatically

Use `[[wikilinks]]` to link between notes. They will be rendered as HTML links.

## Related

- [[welcome]]
